<x-app-layout>
    <x-slot name="header"><h2 class="font-semibold text-xl text-gray-800 leading-tight">{{ __('Review Disciplinary Incidents') }}</h2></x-slot>
    <div class="space-y-6">@forelse($incidentReports as $incident)<div class="bg-white shadow-sm rounded-lg p-6"><div class="flex justify-between gap-4"><div><h3 class="font-bold text-gray-900">{{ $incident->student->lrn }} @if($incident->student->full_name) — {{ $incident->student->full_name }} @endif</h3><p class="text-sm text-gray-600">{{ $incident->offense_type }} ({{ $incident->severity }}) reported by {{ $incident->teacher->name }}</p><p class="mt-2 text-sm text-gray-700">{{ $incident->narrative }}</p></div><div class="text-sm text-gray-500">{{ $incident->incident_date?->format('M d, Y') }}</div></div><form action="{{ route('counselor.incidents.resolve', $incident) }}" method="POST" class="mt-4 grid grid-cols-1 md:grid-cols-4 gap-4 items-end">@csrf @method('PUT')<select name="status" class="rounded-md border-gray-300 shadow-sm"><option value="in_intervention">In Intervention</option><option value="resolved">Resolved</option><option value="escalated">Escalated</option></select><input type="text" name="intervention_type" placeholder="Intervention Type" class="rounded-md border-gray-300 shadow-sm"><input type="datetime-local" name="scheduled_at" class="rounded-md border-gray-300 shadow-sm"><input type="text" name="principal_action" placeholder="Principal Action (optional)" class="rounded-md border-gray-300 shadow-sm"><textarea name="notes" rows="2" class="md:col-span-3 rounded-md border-gray-300 shadow-sm" placeholder="Counselor notes" required></textarea><button type="submit" class="inline-flex rounded-md bg-amber-600 px-4 py-2 text-sm font-medium text-white">Update Case</button></form></div>@empty<div class="bg-white shadow-sm rounded-lg p-6 text-sm text-gray-500">No active incidents to review.</div>@endforelse</div>
</x-app-layout>
