<x-app-layout>
    <x-slot name="header"><h2 class="font-semibold text-xl text-gray-800 leading-tight">{{ __('Violator Database') }}</h2></x-slot>
    <div class="bg-white shadow-sm rounded-lg p-6">
        <form method="GET" action="{{ route('counselor.violators.index') }}" class="mb-4"><input type="text" name="q" value="{{ $search }}" placeholder="Search by LRN" class="rounded-md border-gray-300 shadow-sm w-full md:w-72"></form>
        <table class="min-w-full divide-y divide-gray-200"><thead class="bg-gray-50"><tr><th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Student</th><th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Total Offenses</th><th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Latest Infraction</th><th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Good Moral Status</th></tr></thead><tbody class="divide-y divide-gray-200 bg-white">@forelse($students as $student)<tr><td class="px-6 py-4 text-sm text-gray-900">{{ $student->lrn }} @if($student->full_name) — {{ $student->full_name }} @endif</td><td class="px-6 py-4 text-sm text-gray-600">{{ $student->incident_reports_count }}</td><td class="px-6 py-4 text-sm text-gray-600">{{ optional($student->incidentReports->first())->offense_type ?? '—' }}</td><td class="px-6 py-4 text-sm {{ $student->isEligibleForGoodMoral() ? 'text-green-700' : 'text-red-700' }}">{{ $student->isEligibleForGoodMoral() ? 'Eligible' : 'Blocked' }}</td></tr>@empty<tr><td colspan="4" class="px-6 py-6 text-sm text-gray-500">No violator records found.</td></tr>@endforelse</tbody></table>
    </div>
</x-app-layout>
