<x-app-layout>
    <x-slot name="header"><h2 class="font-semibold text-xl text-gray-800 leading-tight">{{ __('DLL Review Queue') }}</h2></x-slot>
    <div class="space-y-4">@forelse($dailyLessonLogs as $dailyLessonLog)<div class="bg-white shadow-sm rounded-lg p-6"><div class="flex justify-between gap-4"><div><h3 class="font-bold text-gray-900">{{ $dailyLessonLog->subject }}</h3><p class="text-sm text-gray-600">{{ $dailyLessonLog->author?->name }} — {{ $dailyLessonLog->target_date?->format('M d, Y') }}</p></div><span class="text-sm capitalize">{{ $dailyLessonLog->status }}</span></div><div class="mt-4 flex flex-wrap gap-3"><form method="POST" action="{{ route('school_head.dll.approve', $dailyLessonLog) }}">@csrf @method('PUT')<button class="rounded-md bg-emerald-600 px-4 py-2 text-sm font-medium text-white">Approve</button></form><form method="POST" action="{{ route('school_head.dll.return', $dailyLessonLog) }}" class="flex flex-wrap gap-2 items-center">@csrf @method('PUT')<input type="text" name="review_feedback" placeholder="Return feedback" class="rounded-md border-gray-300 shadow-sm" required><button class="rounded-md bg-red-600 px-4 py-2 text-sm font-medium text-white">Return</button></form></div></div>@empty<div class="bg-white shadow-sm rounded-lg p-6 text-sm text-gray-500">No DLL submissions found.</div>@endforelse</div>
</x-app-layout>
