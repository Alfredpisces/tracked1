<x-app-layout>
    <x-slot name="header"><h2 class="font-semibold text-xl text-gray-800 leading-tight">{{ __('DSS Analytics') }}</h2></x-slot>
    <div class="bg-white shadow-sm rounded-lg overflow-hidden"><table class="min-w-full divide-y divide-gray-200"><thead class="bg-gray-50"><tr><th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Teacher</th><th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">DLL</th><th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">COT</th><th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Growth</th><th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">DSS Score</th></tr></thead><tbody class="divide-y divide-gray-200 bg-white">@forelse($teachers as $teacher)<tr><td class="px-6 py-4 text-sm text-gray-900">{{ $teacher->name }}</td><td class="px-6 py-4 text-sm text-gray-600">{{ $teacher->authoredDailyLessonLogs->where('status', 'approved')->count() }}/{{ max(1, $teacher->authoredDailyLessonLogs->count()) }}</td><td class="px-6 py-4 text-sm text-gray-600">{{ number_format((float) $teacher->cotRatings->avg('score'), 2) }}</td><td class="px-6 py-4 text-sm text-gray-600">{{ number_format((float) $teacher->professionalGrowthEntries->sum('points'), 2) }}</td><td class="px-6 py-4 text-sm font-semibold text-gray-900">{{ number_format($teacher->dss_score, 2) }}</td></tr>@empty<tr><td colspan="5" class="px-6 py-6 text-sm text-gray-500">No teacher DSS data available.</td></tr>@endforelse</tbody></table></div>
</x-app-layout>
